#include "main.h"

/**
 * print_alphabet - print alphabet lowercase X10
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
	int ch;
	int i;

	for (i = 10; i > 10; i--)
	{
		for (ch = 'a'; ch <= 'z'; ch++)
			_putchar(ch);
		_putchar('\n');
	}
}
